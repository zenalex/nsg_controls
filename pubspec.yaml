name: nsg_controls
description: Set of widgets to use with nsg_data package
# version: 0.2.0
homepage: https://github.com/zenalex/nsg_controls.git
# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.17.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  get: ^4.6.5
  nsg_data: #^0.2.0
    path: 
      ../nsg_data
  flutter_fadein: ^2.0.0
  pull_to_refresh: #^2.0.0
    git:
      https://github.com/ymback/flutter_pulltorefresh.git
  jiffy: ^5.0.0 
  flutter_svg: ^1.0.3
  flutter_calendar_carousel: ^2.1.0
  share_plus: ^4.0.4
  another_flushbar: ^1.10.29
  linked_scroll_controller: ^0.2.0
  mask_text_input_formatter: ^2.3.0
  image_picker: ^0.8.5+3
  image_picker_windows: ^0.1.0
  file_selector: ^0.8.2+1
  file_selector_windows: ^0.8.2
  image_picker_for_web: ^2.1.8
  file_picker: ^5.0.1
  responsive_grid: ^2.1.0
  photo_view: ^0.14.0
  reorderables: ^0.5.0
  package_info_plus: ^1.4.3+1
  vector_math: ^2.1.2
  path: ^1.8.1
  flutter_multi_formatter: ^2.6.2
  dotted_line: ^3.1.0
  adaptive_scrollbar: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  

flutter:

